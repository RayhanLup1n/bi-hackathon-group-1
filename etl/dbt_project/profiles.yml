pihps_pipeline:
  target: prod
  outputs:
    prod:
      type: duckdb
      path: "{{ env_var('DUCKDB_PATH', '/opt/airflow/data/pihps.duckdb') }}"
      threads: 4
      extensions:
        - httpfs
        - parquet
